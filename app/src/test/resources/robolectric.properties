# Robolectric 全局默认模拟 SDK。
# 工程 compileSdk=36，但单测只需一个稳定的 Android 运行时来提供真实 org.json / android.util.Log，
# 与被测逻辑（DomainResolver 等）无关；固定到 34 避免按 compileSdk 拉取更大的 android-all jar，
# 各测试类已显式 @Config(sdk=[34]) 双保险。
sdk=34
