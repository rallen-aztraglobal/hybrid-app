# Play Store Listing — CalcPad

## App Title (max 30 chars)
CalcPad

> 备选（若 `CalcPad` 在 Play 上重名或被占）：`CalcPad — Calculator`（21 字符）。
> 商店名不要直接叫 `Calculator`：过于通用，搜不到、也容易被判为误导性命名。

## Short Description (max 80 chars)
A clean, fast calculator for everyday math. No ads, no clutter, no sign-in.

## Full Description (max 4000 chars)
CalcPad is a straightforward calculator for the arithmetic you actually do every day.

What it does:
- Add, subtract, multiply and divide
- Percentages
- Sign toggle for negative numbers
- Decimal point
- Backspace to fix a single mistake, AC to start over

Why you might like it:
- Big round keys sized for one-handed use
- The equation you are building stays visible above the result, so you can see
  what you typed before you commit to it
- Dark, low-glare interface that is easy on the eyes at night
- Opens instantly and does nothing else in the background
- No account, no sign-in, no onboarding
- Portrait layout, works offline

That is the whole app. No scientific mode you never use, no history you have to
manage, no banners covering the keypad. Type the numbers, read the answer, close it.

## Category
Tools

## Tags / keywords
calculator, simple calculator, basic calculator, percentage calculator,
dark calculator, offline calculator

## Contact
- Support email: **TODO —— 必须是真实可达的邮箱**
- Privacy policy URL: **TODO —— 必须是公网可访问的 URL**

> 这两项是 Play 必填。`store/privacy-policy.html` 是可直接部署的隐私政策页面，
> 挂到任意静态托管（Netlify / GitHub Pages / 对象存储）拿到 URL 即可。
> hexacolorsort 用的是 Netlify（`https://stalwart-khapse-3231a0.netlify.app`），
> 本包**不要复用同一个 URL** —— 两个上架包指向同一个隐私政策页面，等于在 Play 侧
> 把它们关联起来，与各包用互不相关厂商命名空间的初衷相冲突。
