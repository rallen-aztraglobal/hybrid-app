# deploy/ 本地配置与机密——绝不提交（护栏 #4 / ADR-0008）
# 实际环境变量（含口令）。只提交 *.example（含 .env.api.example / .env.console.example）。
.env
.env.*
!.env.example
!.env.*.example

# keystore 及任何签名密钥：仅作构建机 secret，绝不入仓库/镜像/DB/对象存储/前端。
secrets/*
!secrets/README.md
*.keystore
*.jks
*.p12
*.pem
