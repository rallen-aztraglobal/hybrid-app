*.iml
.gradle
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx

# === 渠道中台（server / cli / web）构建产物 ===
/server/bin/
/server/data/
/cli/bin/
/web/node_modules/
/web/dist/
*.tsbuildinfo

# 编辑器 / 工具临时产物
/.vscode/
/.kotlin/
# firebase CLI 调试日志
firebase-debug.log
# FCM service account 私钥（机密）：不进 git（GitHub Push Protection 会拦；ADR-0008）。
# 离线交付（zip）+ release.sh rsync 上服务器即可，部署不依赖 git。
# 仅忽略含 private_key 的 adminsdk 文件；非机密的 google-services-*.json 仍跟踪，供打包烧入 APK。
deploy/google-services/*firebase-adminsdk*