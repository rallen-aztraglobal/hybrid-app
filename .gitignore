*.iml
.gradle
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx

# === 渠道中台（server / cli / web）构建产物 ===
/server/bin/
/server/data/
/cli/bin/
/web/node_modules/
/web/dist/
*.tsbuildinfo
# app/adjust-tokens.json 由 CLI pull/build 渲染（ADR-0013），与 app/google-services.json 同一
# 逻辑（App Token 非机密、随 APK 分发，但属渲染产物不进 git）。google-services.json 的忽略规则
# 在 app/.gitignore（归 app/ 目录代理维护），此文件同属 app/ 但按多代理目录边界改在此处归口忽略。
/app/adjust-tokens.json

# 编辑器 / 工具临时产物
/.vscode/
/.kotlin/
# firebase CLI 调试日志
firebase-debug.log
# FCM service account 私钥（机密）：不进 git（GitHub Push Protection 会拦；ADR-0008）。
# 离线交付（zip）+ release.sh rsync 上服务器即可，部署不依赖 git。
# 仅忽略含 private_key 的 adminsdk 文件；非机密的 google-services-*.json 仍跟踪，供打包烧入 APK。
deploy/google-services/*firebase-adminsdk*
# 全局兜底：任何位置的 adminsdk 私钥都不进 git（含 deploy/hybrid-fcm-service-accounts/）。
*firebase-adminsdk*
deploy/hybrid-fcm-service-accounts/